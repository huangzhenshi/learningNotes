bye/quit 退出ftp 会话

mget install.log   批量获取指定文件到对应的目标路径

lcd C:\Users\010987\Desktop\测试传输 切换本地路径为 ***路径

dir 查看ftp上所有的文件夹和文件
ls  查看当前路径下的所有文件
!dir 查看本地文件夹中的文件和内容

bin  切换传输方式为二进制  速度快
asc  切换传输协议方式为 文本模式 使用 文本传输

put zzs.dmp 将当前本地路径下的zzs.dmp文件 传入到当前ftp路径下面
mput *.*  将当前本地路径下的所有文件 传入到当前ftp路径下面

get zzs.dmp
mget *.*  获取当前路径下的所有文件到 本地的进入文件夹中

prompt off 关闭交互模式，也就是不会让你确认是否执行，例如执行 mput 多个文件的时候，每个文件他都会让你确认是否导出，
	  如果关闭交互模式的话，就直接导出，不需要确认

delete *.* 删除当前路径下的所有文件
mrdir test 删除test文件夹（确保里面是空的才能删除)

help dir 中文解释 dir命令的作用

先进入到要拷贝的路径下面
cd 目标文件夹
ftp  10.7.61.144
校验身份之后
